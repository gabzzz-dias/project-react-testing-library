import React from 'react';
import App from '../App';
import renderWithRoute from '../helper/renderWithRoute';

describe('Request 6', () => {
  describe('if the page render a card with current pokemon infos', () => {
    it('', () => {
      renderWithRoute(<App />);
    });
  });
});
